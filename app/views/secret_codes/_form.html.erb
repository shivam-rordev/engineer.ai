<%= form_for(secret_code) do |f| %>
  <% if secret_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(secret_code.errors.count, "error") %> prohibited this secret_code from being saved:</h2>
      <ul>
      <% secret_code.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class='form-group'>
    <%= f.label :select_count%>
    <%= f.select :number, [1, 10, 20, 50, 100], class: 'form-control'%></br>
  </div>
  <div lass="actions">
    <%= link_to 'Back', secret_codes_path, class: 'btn btn-info' %>
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
